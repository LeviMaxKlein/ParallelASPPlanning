% This template was initially provided by Dulip Withanage.
% Modifications 
% were made by Conny Junghans and Jannik Strötgen
% and Sascha Hunold

\documentclass[
     11pt,                    % font size
     a4paper,             % paper format
     BCOR=10mm,     % binding correction
     DIV=14,                 % stripe size for margin calculation
     listof=totoc,                    % table listing in toc
     bibliography=totoc,       % bibliography in toc
     index=totoc,              % index in toc
%     parskip            % paragraph skip instead of paragraph indent
     twoside,
     headsepline
     ]{scrreprt}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% ============================================================
% ENCODING & FONTS
% ============================================================
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}

% ============================================================
% LANGUAGE & BIBLIOGRAPHY
% ============================================================
\usepackage[backend=biber, style=authoryear, sorting=ynt]{biblatex}
\addbibresource{references.bib}

% ============================================================
% LAYOUT & SPACING
% ============================================================
\usepackage{setspace}
\usepackage{adjustbox}

% ============================================================
% LISTS & ENUMERATIONS
% ============================================================
\usepackage{enumitem}
\setlist{itemsep=1pt, parsep=0pt, topsep=2pt}

% ============================================================
% MATHEMATICS
% ============================================================
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{colonequals}
\counterwithout{equation}{chapter}

% ============================================================
% GRAPHICS & FIGURES
% ============================================================
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning}
\usepackage{subcaption}
\usepackage{caption}

% ============================================================
% CODE LISTINGS
% ============================================================
\usepackage{minted}
\usepackage{scrhack}

% ============================================================
% THEOREMS & DEFINITIONS
% ============================================================
\usepackage{mdframed}

\theoremstyle{definition}
\newmdtheoremenv{theo}{Theorem}

\theoremstyle{definition}
\newtheorem*{definition}{Definition}

\theoremstyle{remark}
\newtheorem{example}{\textit{Example}}
\newtheorem*{remark}{Remark}
\newtheorem*{notation}{Notation}

\newtheoremstyle{bfnote}
  {}{}
  {}{}
  {\bfseries}{.}
  { }{\thmname{#1}\thmnumber{ #2}\thmnote{ \textit{#3}}}
\theoremstyle{bfnote}
\newtheorem{defi}{Definition}

% ============================================================
% HYPERLINKS & REFERENCES
% ============================================================
\usepackage[
  bookmarks=true,
  colorlinks,
  pdfpagelabels,
  pdfstartview=FitH,
  bookmarksopen=true,
  bookmarksnumbered=true,
  linkcolor=blue,
  plainpages=false,
  hypertexnames=false,
  citecolor=blue,
  urlcolor=blue
]{hyperref}

% ============================================================
% UTILITIES
% ============================================================
\usepackage{makeidx}
\usepackage{url}
\usepackage{doc}

% ============================================================
% CUSTOM COMMANDS
% ============================================================
\newcommand*{\logeq}{\ratio\Longleftrightarrow}
\newcommand{\dom}{\mathrm{dom}}

\renewcommand*{\nameyeardelim}{\addspace}

\DeclareCiteCommand{\parencite}
  {\usebibmacro{prenote}}
  {\bibopenparen\printtext[bibhyperref]{\printnames{labelname}\nameyeardelim\printfield{year}}\bibcloseparen}
  {\multicitedelim}
  {\usebibmacro{postnote}}

\DeclareCiteCommand{\textcite}
  {\usebibmacro{prenote}}
  {\printtext[bibhyperref]{\printnames{labelname}\nameyeardelim\bibopenparen\printfield{year}\bibcloseparen}}
  {\multicitedelim}
  {\usebibmacro{postnote}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% OTHER SETTINGS:

% Pagestyle:
\pagestyle{headings}

% Choose language
\newcommand{\setlang}[1]{\selectlanguage{#1}\nonfrenchspacing}

\begin{document}
% TITLE:
% TITLE:
\pagenumbering{roman} 
\begin{titlepage}


\vspace*{1cm}
\begin{center}
\vspace*{3cm}
\textbf{ 
\Large Ruprecht-Karls-Universität Heidelberg\\
\smallskip
\Large Institut für Informatik\\
\smallskip
}

\vspace{3cm}

\textbf{\large Bachelor Thesis}

\vspace{0.5\baselineskip}
{\huge
Parallel ASP Planning
}
\end{center}

\vfill 

{\large
\begin{tabular}[l]{ll}
Name: & Levi Maximilian Klein\\
Student ID: & 4283957\\
Supervisor: & Dr. Gnad\\
Submission date: & 17.02.2026
\end{tabular}
}

\end{titlepage}

\onehalfspacing

\thispagestyle{empty}

\vspace*{100pt}
\noindent
Ich versichere, dass ich diese Bachelor-Arbeit selbstständig verfasst und nur die angegebenen
Quellen und Hilfsmittel verwendet habe.

\vspace*{50pt}

\noindent
Heidelberg, dd.mm.yyyy
\cleardoublepage

\section*{Abstract}

This is the abstract.

%\cleardoublepage

\tableofcontents
\cleardoublepage
\pagenumbering{arabic} 

% List of figures (Abbildungsverzeichnis):
\listoffigures
% List of tables (Tabellenverzeichnis):
\listoflistings
\cleardoublepage

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Introduction}\label{intro}
Automated planning is a fundamental area of Artificial Intelligence that deals with finding a sequence of actions to transition a system from an initial state to a desired goal state. 
Finding an efficient and effective plan is crucial in various applications and industries such as robotics and logistics. 
In general, planning problems are computationally challenging, often falling into NP-hard or even PSPACE-complete complexity classes.
Therefore, developing efficient algorithms and representations for planning tasks to reduce the search space and computation time is of great importance.

One of the strengths of Classical Planning is its domain independence. This means planning techniques can be used in many different fields without adjusting the core methods. This makes classical planning highly flexible and widely applicable.


\section{Motivation}

\section{Goal of the Thesis}

\section{Structure of the Thesis}
Here you describe the structure of the thesis. For example:
In chapter \ref{definitions}, we formally define the notion of planning tasks, first describing sequential plans and afterward three different kinds of parallel plans.
We then present the ASP encodings for the different plans and proofing their soundness.
In chapter \ref{pipeline} we show the pipeline from translating a pddl problem to validating the given plan and introduce different optimization strategies 


\chapter{Theory}\label{definitions}
\section{Introduction to Planning Task and Parallel Planning}

Before discussing the specific encodings and optimizations for parallel planning, we establish a precise mathematical model of the planning problem.

\begin{defi}[Multi-valued planning task]
    \label{def:MVPlan}
    A STRIPS-like multivalued planning task according to \parencite{Helmert2006} is given by a 4 tuple $\langle \mathcal{F}, s_0, s_*, \mathcal{O} \rangle$, where:
    \begin{itemize}
      \item $\mathcal{F}$ is a finite set of state variables, also called \textbf{fluents}. Each $x \in \mathcal{F}$ is associated with a finite domain $\dom(x)$. In any given state, a fluent $x$ is assigned exactly one value $v \in \dom(x)$.
      \item $s_0$, the \textbf{initial state}, is a total function,, s.t. $s_0(x) \in \dom(x)$ for each $x \in \mathcal{F}$, i.e. a function that maps each fluent to a value from its domain.
      \item $s_*$, the \textbf{goal conditions}, is a partial function, s.t. $s_*(x) \in \dom(x)$, for each $x \in \dom(s_*)$ i.e. a function that specifies the desired values for a subset of fluents.
      \item $\mathcal{O}$ is a finite set of operators, also called \textbf{actions}. An action $a \in \mathcal O$ is a tuple $\langle a^c, a^e \rangle$, where $a^c$ is the \textit{precondition} and $a^e$ is the \textit{postcondition} of $a$. 
      Both $a^c$ and $a^e$ are partial functions, such that $a^c(x) \in \dom(x)$ for each $x \in \dom(a^c)$ and $a^e(x) \in \dom(x)$ for each $x \in \dom(a^e)$.
    \end{itemize}
\end{defi}

\begin{remark}
  In many implementations, actions also include \textbf{conditional effects} \textit{eff} $=\langle cond, x, v \rangle$, where the fluent $x \in \mathcal F$ is assigned value $v \in \dom(x)$ iff $cond$, a partial function called the \textit{condition} such that $cond(x) \in \dom(x)$ for each $x \in \dom(cond)$, holds in the current state.
  However, for the definitions and ASP encodings discussed in this thesis, we adopt a simplified view where effect conditions are implicitly assumed to be satisfied (i.e., mapping any effect on $x$ to $a^e(x)$ regardless of its condition). Thus, $a^e$ denotes the union of all variable assignments an action can potentially trigger.
\end{remark}

\begin{notation}
  Formally, the fact that a fluent $X$ is assigned the value $V$ in a state $s$ is denoted by $s(X) = V$.
  For readability, we use the compact notation $X=V$ to denote this assignment when the reference to the specific state $s$ is implicit from the context.
\end{notation}

\begin{example}
  Let us illustrate the definitions with a small example. The plan is to leave the house by opening the door, going outside, and finally closing the door.
  \begin{itemize}
    \item $\mathcal{F} = \{door, location\}$ with $\dom(door) {=} \{open, closed\}$, $\dom(location) {=} \{inside, outside\}$
    \item Initial state: $s_0 = \{door{=}closed, location{=}inside\}$
    \item Goal: $s_* = \{door{=}closed, location{=}outside\}$
    \item Actions: $\mathcal{O} = \{openDoor, closeDoor, goOut\}$, where
    \vspace{-1em}
    \begin{align*}
      openDoor &= \langle\{door{=}closed\}, \{door{=}open\}\rangle\\
      closeDoor &= \langle\{door{=}open\}, \{door{=}closed\}\rangle\\
      goOut &= \langle\{location{=}inside, door{=}open\}, \{location{=}outside\}\rangle
    \end{align*}
  \end{itemize}
\end{example}

Intuitively, fluents can be understood as the dynamic properties that describe the configuration of the world. In our running example, this world is characterized by the \textit{location} of the actor and the status of the \textit{door}. The initial state captures the complete snapshot of the world at the beginning. This stands in contrast to the goal conditions, which represent the desired outcome of the planning task. Unlike the initial state, the goal is typically partial, meaning we only specify the values we explicitly care about, leaving other variables undefined. Finally, actions define the transition logic of the system: they describe how the world changes, where the precondition establishes the necessary requirements for the action to be applicable, and the postcondition specifies the resulting effects.

With these concepts in place, we now formally define the state transition function, which dictates how applying an action transforms one state into another.

\begin{defi}[Successor state]
  Let $s$ be state and $a \in \mathcal{O}$ an action.\\
  The \textit{successor state} $o(a,s)$, obtained by applying action $a$ in state $s$, is defined if the precondtion of $a$ holds, i.e., $a^c(x) = s(x)$ for all $x \in \dom(a^c)$. Then
  $$
  s'(x) = o(a,s) = \begin{cases} a^e(x) & \text{if } x \in \dom(a^e) \\ s(x) & \text{otherwise.} \end{cases}
  $$ 
\end{defi}
\begin{remark}
  Let $A_n = \langle a_1, \dots, a_n \rangle$ be a sequence of actions with length n. We define the application of $A_n$ to a state $s$ recursevily by
  $$
    o(A_n,s) = o(a_n,o(A_{n-1},s))
  $$
  if $o(A_i, s)$ is defined for all $1 \le i \le n$.
\end{remark}

We can represent these transitions in a directed graph, where each node correspond to a distinct state, while the edges represent the possible actions from one state into another. Consequently, a solution equates to finding a path through this graph. 
Graphically, this transition mechanism induces a directed graph structure known as the \textit{state space}.
In this representation, the nodes correspond to the distinct states of the world, while the edges represent the actions transforming one state into another.
Consequently, finding a solution equates to identifying a path through this graph.
Since this graph grows exponentially with the number of actions, efficiently searching through this state space is a central challenge in automated planning.

\begin{defi}[Sequential plan]
  A \textit{sequential plan} is a sequence of actions $A_n = \langle a_1, \dots, a_n \rangle$, s.t. 
  $s' = o(A_n,s_0)$ is defined and $s'(x) = s_*(x)$ for all $x \in \dom(s_*)$.
\end{defi}

\begin{example}
  Consider the planning task from the previous example. One possible sequential plan is $A = \langle openDoor, goOut, closeDoor \rangle$. Since
  \begin{align*}
    o(A, s_0) &= o(\langle openDoor, goOut, closeDoor\rangle, \{door{=}closed, location{=}inside\})))\\
    &= o(\langle goOut, closeDoor \rangle, \{door{=}open, location{=}inside\}))\\
    &= o(closeDoor, \{door{=}open, location{=}outside\})\\
    &= \{door{=}closed, location{=}outside\} = s_*
  \end{align*}
\end{example}
\hspace{-1.5em}

While the sequential approach is intuitive, it is often inefficient searching through the whole state space.
A possible way to reduce the horizon required to find a solution is to allow multiple actions to be executed simultaneously, forming a parallel plan.
However, arbitrary sets of actions cannot simply be executed simultaneously, since conflicts may arise if actions interfere with each other.
The most fundamental requirement for a set of actions to be valid is that they must agree on their postcondition.
If two actions executing in parallel attempt to assign different values to the same variable, the resulting state would be undefined.
To prevent this ambiguity, we introduce the concept of \textit{confluence}.


\begin{defi}[confluent]
  Let $A = \{a_1, \dots, a_n\} \subseteq \mathcal{O}$ be a set of actions.\\
  $A$ is \textit{confluent} $\logeq a_i^e(x) = a_j^e(x)$ for all $1 \le i < j \le n$ and each $x \in \dom(a_i^e) \cap \dom(a_j^e)$
\end{defi}

\begin{remark}
  $A$ is confluent $\Leftrightarrow B$ is confluent for all $B \subseteq A$
\end{remark}

\begin{example}
  In our initial example, the set $A = \{openDoor, goOut\}$ is confluent because the domains of their effects are disjoint. Similarly, $\{closeDoor, goOut\}$ is confluent.
  
  To illustrate more complex scenarios, we extend our example with two fluents, $light$ and $key$, where $\dom(light) = \dom(key) = \{0, 1\}$, and two corresponding actions:
  \begin{align*}
    lightsOut &= \langle \{location{=}inside, light{=}1\}, \{light{=}0\} \rangle \\
    getKey &= \langle \{location{=}inside, key{=}0\}, \{key{=}1\} \rangle
  \end{align*}
  Additionally, we modify the precondition of $openDoor$ to require the key ($openDoor^c = \{door{=}closed, key{=}1\}$) and update the goal to $s_* = \{location{=}outside, door{=}closed, light{=}0\}$.
  
  In this extended setting, the set $A' = \{getKey, openDoor, goOut, lightsOut\}$ is also a confluent set of actions. Even though the actions are semantically related, their effects target different variables ($key, door, location, light$), so no conflicts in their effects occur.
\end{example}

The concept of confluence ensures that the simultaneous execution of actions within a set $A$ does not lead to contradictory state updates regarding the postcondition of the actions $a \in A$. 
However, ensuring consistent effects is only one part of the problem, since we also need to consider the preconditions of the actions. Unlike in sequential planning, where preconditions are simply checked against the current state, parallel planning allows for different interpretations of precondition dependencies.
These interpretations lead to the different parallel representations of sequential plans, which have been investigated in the literature \parencite{Dimopoulos1997, RINTANEN20061031, Wehrle2007} and are specifically implemented and tested using Answer Set Programming in \parencite{DIMOPOULOS_2019}.


\begin{defi}[Parallel representations]
  Let $s$ be a state and $A = \{a_1, \dots, a_n\}$ be a confluent set of actions. Then $A$ is called:
  \begin{align*}
    \text{(i)}\quad & \text{\textit{$\forall$-step serializable} in $s$} &&\logeq \quad 
      \parbox[t]{0.55\textwidth}{
        $o(\langle a_{\pi(1)}, \dots, a_{\pi(n)} \rangle, s)$ is defined \\ 
        for \textbf{all} permutations $\pi$
      } \\[1em]
    \text{(ii)}\quad & \text{\textit{$\exists$-step serializable} in $s$} &&\logeq \quad 
      \parbox[t]{0.55\textwidth}{
        $o(\langle a_{\pi(1)}, \dots, a_{\pi(n)} \rangle, s)$ is defined \\
        for \textbf{some} permutation $\pi$ and\\
        $a^c(x) = s(x)$ for each $a \in A$, $x \in \dom(a^c)$
      } \\[1em]
    \text{(iii)}\quad & \text{\textit{relaxed $\exists$-step serializable} in $s$} &&\logeq \quad 
      \parbox[t]{0.55\textwidth}{
        $o(\langle a_{\pi(1)}, \dots, a_{\pi(n)} \rangle, s)$ is defined \\
        for \textbf{some} permutation $\pi$
      }
  \end{align*}
\end{defi}

\begin{remark}
  $A$ is $\forall$-step serializable $\Rightarrow$ $A$ is $\exists$-step serializable $\Rightarrow$ $A$ is relaxed $\exists$-step serializable.
\end{remark}

Each parallel representation has different restrictions on how the preconditions of the actions in $A$ are satisfied with $\forall$-step being the most restrictive and relaxed $\exists$-step being the least restrictive. 

\begin{itemize}
    \item \textbf{$\forall$-step serialization} requires strong independence: Since the set must be executable in \textit{any} order, no action is allowed to invalidate the preconditions of another action in the same set and since any action $a \in A$ must be executable at the start, all preconditions have to be satisfied in the current state $s$.
    
    \item \textbf{$\exists$-step serialization} relaxes the independence. Preconditions must still be satisfied in $s$ (i.e., actions cannot enable each other). However, conflicts are allowed: One action $a \in A$ may invalidate the precondition of another action $a' \in A$, provided that there exists \textit{some} valid execution order where $a$ comes after $a'$.
    
    \item \textbf{Relaxed $\exists$-step serialization} is the least restrictive. It drops the requirement that preconditions must hold in $s$. An action $a \in A$ can establish the preconditions required by another action $a' \in A$, as long as they can be ordered such that the $a$ comes before $a'$.
\end{itemize}

Based on these definitions of valid parallel steps, we can now define the corresponding parallel plans.

\begin{defi}[Parallel Plans]
  \label{def:PPlan}
  Let $A = \langle A_1, \dots, A_m \rangle$ be a sequence of confluent sets of actions. $A$ is a \textit{$\forall$-step, $\exists$-step or relaxed $\exists$-step plan} iff
  \begin{itemize}
    \item $s_m(x) = s_*(x)$ for each $x \in \dom(s_*)$
    \item $A_i$ is $\forall$-step, $\exists$-step or relaxed $\exists$-step serializable in $s_{i-1}$ for $1 \le i \le m$, where \\
    \[
      s_i(x) = \begin{cases} a^e(x) & \text{if } x \in \dom(a^e) \\ s_{i-1}(x) & \text{otherwise} \end{cases}
    \]
    for each $a \in A_i$.
    
  \end{itemize}
\end{defi}

\begin{example}
  In our modified example we have:
  \begin{itemize}
    \item $\forall$-step plan: $\langle\{getKey, lightsOut\}, \{openDoor\}, \{goOut\}, \{closeDoor\}\rangle$
    \item $\exists$-step plan: $\langle\{getKey, lightsOut\}, \{openDoor\}, \{goOut, closeDoor\}\rangle$
    \item relaxed $\exists$-step plan: $\langle\{getKey, lightsOut, openDoor, goOut\}, \{closeDoor\}\rangle$
  \end{itemize}
\end{example}

While the concept of a sequential plan is straightforward, we are effectively dealing with transitions of the form $s \xrightarrow{a} s'$. 
In parallel planning, however, we generalize this transition to allow sets of actions, i.e., $s \xrightarrow{A} s'$, effectively merging multiple actions together into a single one.

\subsection*{Summary}

In this section, we have established the formal framework for multi-valued planning tasks.
We defined the static components of the world (fluents and states), the dynamic transition logic (applying actions), and the conditions under which sets of actions can be executed in parallel.
With these formal definitions in place, the next chapter will demonstrate how to translate this theoretical framework into concrete Answer Set Programming encodings to solve planning tasks efficiently.

\section{Introduction to ASP} \label{asp encoding}
Answer Set Programming (ASP) is a declarative problem-solving approach oriented towards difficult (primarily NP-hard) search problems. Unlike imperative programming, where the programmer specifies the control flow to compute a solution, ASP follows a declarative paradigm: the problem is described as a logic program such that its valid models—called \textit{answer sets}—correspond to the solutions of the original problem. This methodology allows for a clean separation between the problem description (the logic program) and the solving algorithms (implemented by ASP systems like \textit{clingo}).

The theoretical foundation of ASP lies in the stable model semantics of logic programming \parencite{gel88}. To formally define these programs, we first introduce their basic building blocks. The most fundamental units are \textit{atoms} of the form $p(t_1, \dots, t_n)$ or $p$, which can either be true or false. 
These atoms are combined to form literals. A literal is defined as either an atom $A$ (a positive literal) or its default negation $not$ $A$ (a negative literal). Here, $not$ denotes negation as failure, meaning that $not$ $A$ is considered true if no proof for $A$ can be derived from the program.


\subsection*{Logic Programs}
The head of a rule or fact is an atom, which has the same syntatic form as a constant or a function, i.e. $prec(X,V,t)$.
The body consists of a (possible empty) list of literals $L_1, \dots L_n$. Each $L_i$ is of the form $A$ or $not~A$, where $A$ is an atom and $not$ the default negation.
The ''$:$-'' operator can be read as ''$\Leftarrow$'', therefore a rule can be interpreted as "if all literals in the body are true, then the head is true".
A fact is a rule without a body, meaning that the head atom is unconditionally true. An integrity constraint is a rule without a head, that filters solution candidates, meaning that all the literal in its body must not be satisfied, i.e. at least one literal in the body must be false.
A set of atoms is called a $model$ of a logic program, if all facts, rules and integrity constraints are satisfied. 
Atoms are considered true if they are in the model, otherwise they are considered false.
In ASP, a model is called an $answer set$ or $stable model$, if every atom in the model has an acyclic derivation of the program.


Based on these definitions, we can specify the structure of the programs used to model planning tasks. A \textit{normal logic program} consists of a finite set of rules, facts, and integrity constraints. These constructs generally follow the form:
\begin{align*}
   \textbf{Fact:} & \quad A_0. \\
   \textbf{Rule:} & \quad A_0 :\text{- } L_1, \dots, L_n. \\
   \textbf{Constraint:} & \quad \phantom{A_0} :\text{- } L_1, \dots, L_n.
\end{align*}
The \textit{head} of a rule is an atom $A_0$. The \textit{body} consists of a (possibly empty) conjunction of literals $L_1, \dots, L_n$. 
The symbol ``\texttt{:-}'' stands for logical implication ($\leftarrow$) and allows the rule to be interpreted as: "if all literals in the body are true, then the head must be true".

We distinguish three types of rules:
\begin{itemize}
    \item A \textbf{fact} is a rule with an empty body, implying that the head atom $A_0$ is unconditionally true.
    \item A \textbf{rule} derives the head $A_0$ if the body is satisfied.
    \item An \textbf{integrity constraint} is a rule with an empty head. It serves as a filter for solution candidates: At least one literal in the body must be false,otherwise the candidate model is rejected.
\end{itemize}

A set of atoms is called a model of a logic program if it satisfies all rules, facts, and integrity constraints. Atoms are considered true iff
they are contained in the model. In ASP, a model is called an answer set if every atom in the
model has an acyclic derivation from the program.
Based on these semantics, we can now translate the formal definition of a planning task into an ASP logic program. 
The problem instance is represented purely by facts:
the initial state $s_0$ is defined via \texttt{init/2}, the goal $s_*$ via \texttt{goal/2}, and the pre- and postconditions of actions via \texttt{prec/3} and \texttt{post/3} respectively.
Listing \ref{example} shows the complete fact representation for our running example.

\begin{listing}[H]
  \caption{ASP fact representation of the example planning task}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos]{prolog}{encodings/example.lp}
  \label{example}
\end{listing}

\noindent Combining these facts with the right encodings, stable models correspond to the \textit{sequential, $\forall$-step, $\exists$-step} or \textit{relaxed $\exists$-step plans}.

\begin{listing}[H]
  \caption{Core for sequential and parallel encodings for planning}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos,lastline=9]{prolog}{encodings/EX_common.lp}
  \label{core}
\end{listing}
All planning algorithms presented in this work share a common incremental encoding using the \textit{incmode} feature of \textit{clingo} \parencite{clingo14}.
The program is divided into three subprograms: \textit{base}, \textit{step(t)} and \textit{check(t)}.

The \textbf{\textit{base}} subprogram (Line 2) defines the initial state $s_0$, grounding the fluent values at time step $0$. The predicate $holds(X,V,t)$ is used to represent fluents $X \in \mathcal{F}$ that have value $V\in \dom(X)$ at timestep $t$.

The \textbf{\textit{check(t)}} subprogram (Line 4) is a parametrized subprogram starting at $t=0$. The integrity constraint checks if the goald condition is met, i.e. $s_*(X)=s_t(X)$ for each $X\in \dom(s_*)$. The external atom $query(t)$ ensures this check is only active for the current search horizon $t$.

The \textbf{\textit{step(t)}} subprogram (Line 6-9) is grounded for each timestep $t$ starting at $t=1$. 
Note that postconditions of applied actions in timestep $t$ become effective in $t$, while their preconditions have to hold in $t-1$.
Line 6 generates the state candidates for timestep $t$, assigning every fluent $X$ exactly one value $V$ from its domain.
Line 7 generates arbitrary sets of actions $occurs(A,t)$ for the current timestep. These actions and the generated state must obey the constraints in Lines 8 and 9.
Line 8 ensures that the postcondition of an occurring action is actually applied in the state. This also asserts the confluence of the set of actions: if the postconditions of two actions have different values for the same fluent ($V = a^e(X) \ne a'^e(X) = V'$), the uniqueness of the value in the choice rule (Line 6) would be violated.
Finally, Line 9 guarantees that fluents not affected by any action keep their value from the previous timestep. It rejects any change in a fluent's value that is not explained by an action.

In summary, Listing \ref{core} establishes the state transitions consistent with the postcondition of the chosen actions. 
However, it does not yet verify the \textit{applicability} of these actions regarding their preconditions, nor does it enforce the specific serializability requirements (e.g., acyclicity) of the parallel semantics. 

\subsection{Extension for sequential plans}

To restrict the generic transition system to valid \textit{sequential plans}, we need to ensure that preconditions are satisfied and that each execution is restricted to a single action at a time.
This is achieved by the extension shown in Listing \ref{seq}.

\begin{listing}[H]
  \caption{Extension of Listing \ref{core} for encoding sequential plans}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos, firstline=2, firstnumber=1]{prolog}{encodings/EX_seq.lp}
  \label{seq}
\end{listing}

Line 2 implements the applicability check: it enforces that for any action occurring at timestep $t$, all its preconditions must hold in the preceding state $s_{t-1}$.
Line 3 ensures that only one action can occur in each timestep $t$.

\begin{example}
Running this with our example, we get three possible sequential plans.
\begin{listing}[H]
\caption{Output from clingo for sequential encoding}
\begin{minted}[frame=single,framesep=2mm,baselinestretch=1.2,fontsize=\footnotesize]{console}
Answer: 1
occurs(getKey,1) occurs(lightOut,2) occurs(openDoor,3) occurs(goOut,4) occurs(closeDoor,5)
Answer: 2
occurs(getKey,1) occurs(openDoor,2) occurs(lightOut,3) occurs(goOut,4) occurs(closeDoor,5)
Answer: 3
occurs(lightOut,1) occurs(getKey,2) occurs(openDoor,3) occurs(goOut,4) occurs(closeDoor,5)
\end{minted}
\end{listing}
\end{example}

\subsection{Extension for \texorpdfstring{$\boldsymbol{\forall}$}{forall}-step plans}

\begin{listing}[H]
  \caption{Extension of Listing \ref{core} for encoding $\forall$-step plans}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos, firstline=2, firstnumber=1]{prolog}{encodings/EX_forall.lp}
  \label{forall}
\end{listing}

The extension for $\forall$-step plans is shown in Listing \ref{forall}.
Line 1 is identical to the sequential encoding, ensuring that the preconditions of all chosen actions are satisfied in the previous state $s_{t-1}$.
To create a valid $\forall$-step plan, we must ensure that each set of actions is executable in \textit{any} order. 
Since Line 8 of the common core (Listing \ref{core}) already enforces confluence, the remaining requirement is that no action is allowed to invalidate the preconditions of another action in the same set.
We distinguish two types of interactions between an action $a$ and a fluent $X$ in its precondition ($\dom(a^c)$):
\begin{enumerate}
  \item The action has $X$ in its preconditions fluent $X$ in its precondition but does not change it, (i.e. $X \in \dom(a^c) \cap \dom(a^e)$ and $a^c(X) = a^e(X)$ or $X \in \dom(a^c)$ and $X \notin \dom(a^e)$). 
  \item The action has fluent $X$ in its preconditions and changes its value (i.e., $X \in \dom(a^e)$ and $a^c(X) \ne a^e(X)$). 
\end{enumerate}
Line 2 handles case 1 by ensuring that $X$ has to remained unchanged. 
However, case 2 requires strict isolation of these kinds of actions. 
If an action $a$ invalidates its own precondition, no other action $a'$ in the same timestep is allowed to modify $X$. 
If such an action $a'$ existed, the execution order $\langle a', a \rangle$ would fail because $a'$ would change $X$, invalidating the precondition of $a$.
To enforce this, Line 3 marks such actions using the predicate \texttt{single}$(X,t)$. 
The integrity constraint in Line 4 then ensures that for any fluent $X$, at most one such action occurs per time step, preventing any interference.

\begin{example}
  Running this with our example, we get two possible $\forall$-step plans.
\begin{listing}[H]
\caption{Output from clingo for $\forall$-step encoding}
\begin{minted}[frame=single,framesep=2mm,baselinestretch=1.2,fontsize=\footnotesize]{console}
Answer: 1
occurs(getKey,1) occurs(openDoor,2) occurs(lightOut,2) occurs(goOut,3) occurs(closeDoor,4)
Answer: 2
occurs(lightOut,1) occurs(getKey,1) occurs(openDoor,2) occurs(goOut,3) occurs(closeDoor,4)
\end{minted}
\end{listing}
\end{example}

\subsection{Extension for \texorpdfstring{$\boldsymbol{\exists}$}{exists}-step plans}

\begin{listing}[H]
  \caption{Extension of Listing \ref{core} for encoding $\exists$-step plans}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos, firstline=2, firstnumber=1]{prolog}{encodings/EX_exists.lp}
  \label{exists}
\end{listing}
\begin{listing}[H]
  \caption{Alternative Extension for $\exists$-step plans}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos, firstline=2, firstnumber=1]{prolog}{encodings/EX_exists_alt.lp}
  \label{exists_alt}
\end{listing}
\noindent There are two different ways to encode the extension for $\exists$-step plans as seen in Listing \ref{exists} and Listing \ref{exists_alt}.
As before, Line 1 in Listing \ref{exists} and \ref{exists_alt} is the same as in the sequential and $\forall$-step encoding.
To ensure that there is at least one valid permutation, either an action $a_1$ that invalidates the precondition of another action $a_2$ has to be applied after $a_2$
or action $a_2$ does not occur in this timestep.
For this, all actions has to be marked as $ready$ (Line 6). An action is $ready$ if it either does not occur in this timestep (Line 4) or if it is captured by $apply$ (Line 2). 
An action can be safely applied, if all other actions whose preconditions it invalidates are ready, i.e. are either applied beforehand or not occurring in this timestep.
Finally, Line 6 ensures that actions does not circularly invalidates each other.
%TODO explain alternative encoding


\textcite{DIMOPOULOS_2019} presents two different approaches to encode the extension for $\exists$-step plans, shown in Listings \ref{exists} and \ref{exists_alt}.
As before, Line 1 in both listings enforces the standard precondition check.
The difference lies in how they enforce the serializability requirement: the set of parallel actions must allow for at least one valid permutation where no action invalidates the precondition of a subsequent action.

Listing \ref{exists_alt} implements a graph-based approach.
Since the common core (Listing \ref{core}) already establishes confluence, the only remaining requirement is the existence of a valid execution order. This effectively means excluding circular interference among the chosen actions.

This lends itself to an implementation using the \texttt{\#edge} directive \parencite{gebser_et_al:OASIcs.ICLP.2016.2} of \textit{clingo}, where built-in acyclicity checking \parencite{bomanson2016} is utilized.

The underlying structure corresponds to the \textit{conflict graph}. Following the definition in \textcite{DIMOPOULOS_2019}, we define the graph over all possible actions, with edges originating from the currently active set.
\begin{defi}[Conflict Graph]
  Let $A_t$ be the set of actions occurring at time $t$. The conflict graph $G_t = (\mathcal{O}, E)$ for timestep $t$ is a directed graph where
  \[
    E = \{\langle a, a' \rangle \mid \{a, a'\} \in A_t \times \mathcal{O} \setminus \{a\}, \exists X \in \dom(a^e) \cap \dom(a'^c): a^e(X) \ne a'^c(X)\}
  \]
\end{defi}
Intuitively, an edge $(a, a') \in E$ exists if action $a$ invalidates a precondition of action $a'$.
Line 2 of Listing \ref{exists_alt} directly translates these edges into the \texttt{\#edge} syntax. 
A valid serialization of the actions corresponds to a reversed topological ordering of the subgraph induced by $A_t$.
Consequently, any answer set containing a cycle within the set of occurring actions is automatically rejected by the solver's cycle-checking logic.

Listing \ref{exists}, on the other hand, implements this acyclicity checking by a recursive derivation of the predicates \texttt{apply} and \texttt{ready}. 
Line 4 defines the base case: Any action that does not occur is considered $ready$ (since it does not interfere with the execution order).
Line 5 marks applied actions: If an action is successfully applied, it is also \texttt{ready}.
Line 2-3 implements the ordering logic. An action $a_1$ can only be applied (\texttt{apply}) if all other actions $a_2$ whose preconditions it invalidates are already applied or do not occur (\texttt{ready}). 
Line 6 is the final constraint: It requires that every occurring action must eventually become $ready$.
If the subgraph induced by $A_t$ contains a cycle (e.g., $a_1$ invalidates $a_2$ and $a_2$ invalidates $a_1$), neither action can ever be derived as \texttt{apply}, because each would wait for the other to be ready first. Consequently, the constraint in Line 6 would be violated, causing the solver to reject the model.

\begin{example}
  Running this with our example, we get three possible $\exists$-step plans.
\begin{listing}[H]
\caption{Output from clingo for $\exists$-step encoding}
\begin{minted}[frame=single,framesep=2mm,baselinestretch=1.2,fontsize=\footnotesize]{console}
Answer: 1
occurs(getKey,1) occurs(openDoor,2) occurs(closeDoor,3) occurs(goOut,3) occurs(lightOut,3)
Answer: 2
occurs(getKey,1) occurs(openDoor,2) occurs(lightOut,2) occurs(closeDoor,3) occurs(goOut,3)
Answer: 3
occurs(lightOut,1) occurs(getKey,1) occurs(openDoor,2) occurs(closeDoor,3) occurs(goOut,3)
\end{minted}
\end{listing}
\end{example}

\subsection{Extension for relaxed \texorpdfstring{$\boldsymbol{\exists}$}{exists}-step plans}
\begin{listing}[H]
  \caption{Extension of Listing \ref{core} for encoding relaxed $\exists$-step plans}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos, firstline=2, firstnumber=1]{prolog}{encodings/EX_relaxed.lp}
  \label{relaxed_exists}
\end{listing}

Finally, we present the extension from \cite{DIMOPOULOS_2019} for relaxed $\exists$-step plans in Listing \ref{relaxed_exists}.
Structurally, it shares the same derivation approach with the standard $\exists$-step encoding, but it relaxes the applicability check.
Previously, preconditions strictly had to hold in the previous state $s_{t-1}$. 
Here, actions within the same parallel set can provide "internal support" for each other. 
This means an action is applicable if its preconditions are established either by the previous state or by another action occurring earlier in the valid execution sequence of the current step.

This logic is implemented as follows:
The predicate \texttt{reach}$(X,V,t)$ is introduced to represent the values of fluents that are currently available. A value is reachable if it holds in the previous state (Line 1) or if it is obtained by an action that has already been applied in the current internal sequence (Line 2).
The \texttt{apply} rule now checks against this \texttt{reach} predicate instead of the static state. An action can be safely applied if its preconditions are reachable.
As in the standard $\exists$-step encoding, we must still check acyclicity. The integrity constraint in Line 7 ensures that no circular dependencies exist, guaranteeing a valid serialization exists.

\begin{example}
  Running this with our example, we get three possible \textit{relaxed} $\exists$-step plans.
\begin{listing}[H]
\caption{Output from clingo for relaxed $\exists$-step encoding}
\begin{minted}[frame=single,framesep=2mm,baselinestretch=1.2,fontsize=\footnotesize]{console}
Answer: 1
occurs(openDoor,1) occurs(getKey,1) occurs(closeDoor,2) occurs(goOut,2) occurs(lightOut,2)
Answer: 2
occurs(openDoor,1) occurs(lightOut,1) occurs(getKey,1) occurs(closeDoor,2) occurs(goOut,2)
Answer: 3
occurs(openDoor,1) occurs(goOut,1) occurs(lightOut,1) occurs(getKey,1) occurs(closeDoor,2)
\end{minted}
\end{listing}
\end{example}

\section{Proof of Correctness}
In order to prove the correctness of the presented encondings, \cite{DIMOPOULOS_2019} defines the following:
\begin{itemize}
  \item $B$ is the rule in Line 1 of Listing \ref{core}.
  \item $Q(t)$ is the integrity constraint in the check program directive (Line 5) of Listing \ref{core}.
  \item $S(t)$ are the rules and integrity constraint in the step program directive (Lines 6-9) of Listing \ref{core}.
  \item $S^p(t)$ is the extension for the specific plans,
        where $p = s$ for Listing \ref{seq}, $p=\forall$ for Listing \ref{forall}, $p=\exists$ for Listing \ref{exists},
        $p=E$ for Listing \ref{exists_alt} and $p=R$ for Listing \ref{relaxed_exists}.
  \item $I$ is the set of facts representing a planning task $\langle \mathcal{F}, s_0, s_*, \mathcal{O} \rangle$.
  \item $\langle a_1, \dots, a_n \rangle$ is a sequence of actions.
  \item $\langle A_1, \dots, A_m \rangle$ is a a sequence of sets of actions.
  \item $P^p(n) := I \cup B \cup Q(0) \cup \bigcup_{t=1}^{n} \left( Q(t) \cup S(t) \cup S^p(t) \right) \cup \{\texttt{query}(n). \}$, where $p \in \{s, \forall, \exists, E, R\}$.
\end{itemize}


Having presented the ASP encodings for sequential and the different parallel semantics, we now proceed to formally verify their correctness.
The goal is to establish a one-to-one correspondence between the stable models of our logic programs and the valid plans as defined in Definition \ref{def:PPlan}.
We adopt the notation for the proof from \textcite{DIMOPOULOS_2019} and decompose the logic programs into their constituent parts.

\begin{itemize}
  \item $I$ denotes the set of facts representing the specific planning task instance $\langle \mathcal{F}, s_0, s_*, \mathcal{O} \rangle$ (as in Definition \ref{def:MVPlan}).
  \item $B$ corresponds to the rules in the \texttt{base} subprogram (Listing \ref{core}, Line 2).
  \item $Q(t)$ represents the goal verification rules defined in the \texttt{check(t)} subprogram (Listing \ref{core}, Line 4).
  \item $S(t)$ denotes the transition rules (generator and core constraints) defined in the \texttt{step(t)} subprogram (Listing \ref{core}, Lines 6--9).
  \item $S^p(t)$ is the specific extension for timestep $t$, where:
    \begin{itemize}
        \item $p = \text{s}$ for the sequential extension (Listing \ref{seq}),
        \item $p = \forall$ for the $\forall$-step extension (Listing \ref{forall}),
        \item $p = \exists$ for the constructive $\exists$-step extension (Listing \ref{exists}),
        \item $p = \text{E}$ for the graph-based $\exists$-step extension (Listing \ref{exists_alt}),
        \item $p = \text{R}$ for the relaxed $\exists$-step extension (Listing \ref{relaxed_exists}).
    \end{itemize}
\end{itemize}

Using these components, the complete logic program for a horizon $n$ is defined as:
\[
  P^p(n) := I \cup B \cup \bigcup_{t=1}^{n} \left( S(t) \cup S^p(t) \cup Q(t) \right) \cup \{ \texttt{query}(n). \}
\]


\begin{theo}[Correctness of sequential plans \parencite{DIMOPOULOS_2019}]
  \label{theo:1}
  Let $A_n := \langle a_1, \dots, a_n \rangle$, $a_t \in \mathcal{O}$ for $1 \le t \le n$, be a sequence of actions. Then,
  \begin{align*}
      A_n \text{ is a sequential plan} \iff &P^s(n) \text{ has a stable model } M \text{ such that} \\
        &\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M \} = \{\langle a_t, t \rangle \mid 1 \le t \le n \}.
  \end{align*}
\end{theo}


\begin{proof}
  We prove both directions separately.
  \begin{itemize}
  \item[($\Rightarrow$):] 
    Assume $A_n = \langle a_1, \dots, a_n \rangle$ is a sequential plan. \\
    We construct a candidate set $M$ based on the plan execution and show it is indeed a stable model of $P^s(n)$.
    The rule in B ensures that \texttt{holds}(X,V,0) $= s_0(X)$ for each $X \in \mathcal F$. The choice rule (Line 5 of Listing \ref{seq}) enforces that for any $0 \le t \le n$ and $X \in \mathcal F$, exactly one value $V$ is assigned, i.e., $|\{V \mid \texttt{holds}(X,V,t) \in M\}| = 1$. 
    Moreover, the integrity constraints (Lines 8 and 9) in $S(t)$ dictate the transition logic between states. 
    For any $1 \le t \le n$, the value $V$ of a fluent $X$ is determined by:
    \[
      \texttt{holds}(X,V,t) \leftrightarrow \begin{cases} V = a_t^e(X) & \text{if } X \in \text{dom}(a_t^e) \\ V = s_{t-1}(X) & \text{otherwise} \end{cases}
    \]
    Therefore, the set of \texttt{holds}-atoms in any stable model must satisfy:
    \begin{equation}
      \{ \langle X, V, t \rangle \mid \texttt{holds}(X, V, t) \in M \}  = \{ \langle X, V, t \rangle \mid X \in \mathcal{F}, V {=} o(A_n, s_0)(X), 0 \le t \le n \}
      \label{eq:holds-equivalence}
    \end{equation}
    Since the action sequence is fixed by the plan assumption ($\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M \} = \{\langle a_t, t \rangle \mid 1 \le t \le n \}$), the only candidate for a stable model $P^s(n)$ is:
    \begin{align*}
      M = I &\cup \{ \texttt{occurs}(a_t, t) \mid 1 \le t \le n \} \\
            &\cup \{\texttt{holds}(X, V, t) \mid (X, V, t) \text{ satisfies } \eqref{eq:holds-equivalence}\}    \cup \{\texttt{query}(n)\}.
    \end{align*}
     By the definition of a sequential plan, $o(a_t, s_{t-1})$ is defined for all $1 \le t \le n$, and the goal condition $s_n(X) = s_*$ for each $X \in \dom(s_*)$ is met. Thus, the integrity constraint regarding preconditions (Line 1, Listing \ref{seq}) and the goal check (Line 4, Listing \ref{core}) are satisfied. Since $M$ is a set satisfying all rules and constraints, it is indeed a stable model of $P^s(n)$.

  \item[($\Leftarrow$):]
    Assume that $M$ is a stable model of $P^s(n)$ s.t. $\{\langle a, t \rangle \mid \texttt{occurs(a,t)} \in M \} = \{\langle a_t, t \rangle \mid 1 \le t \le n \}$. \\
    We prove by induction over $t$, that for each $1 \le t \le n$, the following holds:
    \begin{equation}
      \{\langle X, V \rangle \mid \texttt{holds(X,V,t)} \in M \} = \{\langle X, V \rangle \mid X \in \mathcal F, V=o(\langle a_1, \dots, a_{t}\rangle, s_0)(X)\}
      \label{eq:holds-equivalence2}
    \end{equation}
    \textit{Base case }$t=0$: \\
    The rule in B directly implies that $\{\langle X, V \rangle \mid \texttt{holds(X, V, 0)} \in M \} = \{\langle X, V \rangle \mid X \in \mathcal F, s_0(X) = V\}$,
    satisfying the equation for $t=0$. \\
    \textit{Induction step }$t-1 \mapsto t$: \\
    Assume the hypothesis holds for $t-1$, i.e., the state at $t-1$ corresponds to $s_{t-1} = o(\langle a_1, \dots, a_{t-1} \rangle, s_0)$.. The integrity constraint in Line 1 of Listing \ref{seq} in $S^s(t)$ ensures that the successor state $s_t = o(a_{t}, o(\langle a_1, \dots, a_{t-1}\rangle, s_0))$ is defined and the choice rule in Line 6 of Listing \ref{core}, together with the integrity constraints in $S(t)$ guarantee that 
    \[
    \{\langle X, V \rangle \mid \texttt{holds(X,V,t)} \in M\} = \{\langle X, V \rangle \mid X \in \mathcal F, V = o(\langle a_1, \dots, a_{t} \rangle, s_0)(X)\}.
    \]
    Thus \eqref{eq:holds-equivalence2} holds for each $1 \le t \le n$.
    Given \texttt{query(n)}, the integrity constraint in $Q(n)$ yields that $o(\langle a_1, \dots, a_n \rangle, s_0) = s_*(X)$ for each $X \in \dom(s_*)$ and therefore $\langle a_1, \dots a_n\rangle$ is a sequential plan. 
\end{itemize}
\end{proof}

\begin{theo}[Correctness of parallel plans \parencite{DIMOPOULOS_2019}]
  Let $\mathcal A = \langle A_1, \dots, A_m \rangle$ be a sequence of sets of actions, $A_t \subseteq \mathcal{O}$ for $1 \le t \le m$. Then,
  \begin{align*}
      \mathcal A \text{ is a $\forall$-/$\exists$-/R-step plan} \iff &P^p(m) \text{ has a stable model } M \text{ such that} \\
        &\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M \} = \{\langle a, t \rangle \mid a \in A_t, 1 \le t \le m \}
  \end{align*}
\end{theo}

\begin{proof}
  We prove both directions separately.
  \begin{itemize}
    \item[($\Rightarrow$):]
      Assume $\mathcal A = \langle A_1, \dots, A_m \rangle$ is a $\forall$-/$\exists$-/\textit{relaxed} $\exists$-step plan.
      For each $1 \le t \le m$, let $s_t$ be the state defined by applying the set $A_t$ to $s_{t-1}$. Due to the confluence of $A_t$, $s_t$ is well-defined as:
      \[
        s_t(X) = \begin{cases} 
          a^e(X) & \text{for each } a \in A_t \text{ and } X \in \dom(a^e) \\ 
          s_{t-1}(X) & \text{otherwise (inertia)}
        \end{cases}
      \]
      Analogous to the proof of Theorem \ref{theo:1}, consider any stable model $M$ of $P^p(m)$ such that the action occurrences match the plan, i.e. $\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M\} = \{\langle a,t \rangle \mid a \in A_t, 1 \le t \le m \}$.
      The fact rule in $B$, together with the choice rule and integrity constraints in $S(t)$, dictates the values of all fluents based on these actions. Thus, the set of \texttt{holds}-atoms in $M$ is determined as:
      \[
        \{\langle X,V,t\rangle \mid \texttt{holds}(X,V,t) \in M\} = \{\langle X,V,t\rangle \mid X \in \mathcal F, V = s_t(X), 0 \le t \le m\}.
      \]
      We now distinguish the different cases of parallel plans: \\


      \textbf{Case 1:} $\mathcal A$ is a $\forall$-step plan. \\
      For $1 \le t \le m$, the atom \texttt{single}$(X,t)$ defined in $S^\forall(t)$ (Line 2 of Listing \ref{forall}) is derived iff there exists some action $a \in A_t$ that invalidates its own precondition, i.e., $X \in \dom(a^c) \cap \dom(a^e)$ and $a^c(X) \ne a^e(X)$. 
      Hence, $P^\forall(m)$ cannot have any stable model $M$ apart from
      \begin{align*}
        M = I &\cup \{\texttt{occurs}(a,t) \mid a \in A_t, 1 \le t \le m\} \\
              &\cup \{\texttt{holds}(X,V,t) \mid X \in \mathcal F, V = s_t(X), 0 \le t \le m\} \\
              &\cup \{\texttt{single}(X,t) \mid a \in A_t, X \in \dom(a^c) \cap \dom(a^e), a^c(X) \ne a^e(X), 1 \le t \le m \} \\
              &\cup \{\texttt{query}(m).\}
      \end{align*}
      such that $\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M\} = \{\langle a, t \rangle \mid a \in A_t, 1 \le t \le m\}$.
      
      It remains to show that $M$ satisfies the integrity constraints in $S^\forall(t)$.
      Since for any $1 \le t \le m$, the successor state $s_t= o(\langle a_1, \dots, a_k \rangle, s_{t-1})$ is defined for \textit{any} sequence $\langle a_1, \dots, a_k \rangle$ with $\{a_1, \dots, a_k\} = A_t$, no action can interfere with the precondition of another action.
      Consequently, if \texttt{single}$(X,t)$ is derived (meaning an action invalidates its own precondition on $X$), no other action can modify $X$, as this would create a conflict in at least one permutation. Therefore, the integrity constraint restricting the count of actions affecting $X$ alongside \texttt{single}$(X,t)$ is satisfied.
      Analogously, the constraint requiring preconditions to remain stable (Line 1 of Listing \ref{forall}) is satisfied since $s_{t-1}(X)=a^c(X)$ for each $a \in A_t$, $X \in dom(a^c)$ and $1\le t \le m$.
      One can check that the remaining rules and integrity constraints in $S(t)$ are satisfied as well, so $M$ is a stable model of $P^\forall(m)$. \\


      \textbf{Case 2:} $\mathcal A$ is a $\exists$-step plan. \\
      We analyze the encodings $P^\exists$ and $P^E$ separately, starting with the acyclicity-based encoding $P^E$.

      \textit{Analysis of }$P^E$: \\
      By definition of an $\exists$-step plan, for every timestep $1 \le t \le m$, there is a sequence of actions $\langle a_1, \dots a_k\rangle$ with $\{a_1, \dots, a_k\} = A_t$. A valid serialization requires that no action invalidates the precondition of a subsequent action.
      Formally, let $G_t = (A_t, E_t)$ be the conflict graph for timestep $t$, where an edge represents a destroyed precondition:
      \[
        E_t = \{ (a, a') \in A_t \times A_t \mid a \ne a', \exists X \in \dom(a^e) \cap \dom(a'^c) : a^e(X) \ne a'^c(X) \}.
      \]
      Since a valid serialization exists, $\langle a_1, \dots, a_k \rangle$ constitutes a topological ordering of $G_t$, i.e. there exists a sequence of applied actions. A directed graph possesses a topological ordering iff it is a Directed Acyclic Graph (DAG). Thus, $G_t$ must be acyclic.

      Now consider the candidate model $M$ constructed as in Case 1:
      \begin{align*}
        M = I &\cup \{\texttt{occurs}(a,t) \mid a \in A_t, 1 \le t \le m \} \\
              &\cup \{\texttt{holds}(X,V,t) \mid X \in \mathcal F, V=s_t(X), 0 \le t \le m\} \cup \{\texttt{query}(m)\}.
      \end{align*}
      The \texttt{\#edge} directive in Listing \ref{exists_alt} (Lines 2-3) generates an edge between two occurring actions $(a,t)$ and $(a',t)$ exactly if the condition for $E_t$ is met. Consequently, the set of edges declared in the ASP model corresponds exactly to the union of all conflict graphs $\bigcup_{t=1}^m E_t$.
      Since each $G_t$ is acyclic and the graphs for different timesteps $1 \le t < t' \le m$ are vertex-disjoint ($(a,t), \ne (a, t')$), the overall graph induced by $M$ is acyclic.
      Therefore, the acyclicity constraint is satisfied, and $M$ is a stable model of $P^E(m)$. \\
      \textit{Analysis of }$P^\exists$: \\
      The rules defining \texttt{apply} and \texttt{ready} in $S^\exists(t)$ (Lines 2-5 in Listing \ref{exists}) effectively traverse the dependency graph $G_t$.
      Since $G_t$ is acyclic, there exists a topological ordering of the actions. The derivation of atoms proceeds along this ordering: Actions with no dependencies (sinks in $G_t$) are derived as \texttt{apply} first, making them \texttt{ready}. This, in turn, satisfies the conditions for their predecessors. 
      Because $G_t$ is finite and acyclic, this inductive process reaches a fixpoint where \texttt{apply}$(a,t)$ and \texttt{ready}$(a,t)$ hold for all $a \in \mathcal O$ and $1 \le t \le m$.
      Consequently, the integrity constraint requiring all actions to be ready (Line 6 in Listing \ref{exists}) is satisfied.
      Thus, $P^\exists(m)$ extends the base model $M$ to:
      \[
        M' = M \cup \{\texttt{apply}(a,t) \mid a \in \mathcal O, 1 \le t \le m\} \cup \{\texttt{ready}(a,t) \mid a \in \mathcal O, 1 \le t \le m \}
      \]
      such that $\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M'\} = \{\langle a, t \rangle \mid a \in A_t, 1 \le t \le m \}$.
      As established previously, the state transition rules are satisfied by $M$ (and thus by $M'$). Since the additional constraints in $P^\exists$ regarding circularity are also satisfied by the completeness of the \texttt{ready} derivation, $M'$ is indeed a stable model of $P^\exists(m)$. \medskip

      \textbf{Case 3:} $\mathcal A$ is a \textit{relaxed} $\exists$-step plan. \\
      For each $1 \le t \le m$, by definition of a relaxed $\exists$-step plan, there exists a serialization $\langle a_1, \dots, a_k \rangle$ of $A_t$ such that the sequential execution is valid. This means $o(a_i, o(\langle a_1, \dots, a_{i-1} \rangle, s_{t-1}))$ is defined for all $1 \le i \le k$.
      
      Consequently, the preconditions of any action $a_i$ are satisfied by the state resulting from the application of its predecessors in $\langle a_1, \dots, a_n \rangle$.
      The rules in $S^R(t)$ (Listing \ref{relaxed_exists}) simulate this reachability:
      \begin{enumerate}[label=(\roman*)]
        \item The atom \texttt{reach}>$(X, V, t)$ is derived for all values $V$ that are either present in $s_{t-1}$ or produced by an applied action.
        \item The atom \texttt{apply}$(a, t)$ is derived for any action $a \in \mathcal O$ whose preconditions are satisfied by the reachable values.
        \item The atom \texttt{ready}$(a, t)$ is derived for all $a \in \mathcal O$ (either because they are applied, or because they do not occur).
      \end{enumerate} 
      Since the serialization $\langle a_1, \dots, a_k \rangle$ is valid, the least fixpoint of these rules guarantees that \texttt{apply}$(a,t)$ is derived for all $a \in A_t$.
      
      Thus, the stable model $M$ of $P^R(m)$ is:
      \begin{align*}
        M = I &\cup \{\texttt{occurs}(a,t) \mid a \in A_t, 1 \le t \le m \} \\
          &\cup \{\texttt{holds}(X,V,t) \mid X \in \mathcal F, V=s_t(X), 0 \le t \le m \} \\
          &\cup \{\texttt{reach}(X,V,t) \mid X \in \mathcal F, V \in \{s_{t-1}, s_t\}, 1 \le t \le m\} \\
          &\cup \{\texttt{apply}(a,t) \mid a \in \mathcal O: a^c(X) \in \{s_{t-1}(X), s_t(X)\} \text{ for each } X \in \dom(a^c)\} \\
          &\cup \{\texttt{ready}(a,t) \mid a \in \mathcal O, 1 \le t \le m\} \\
          &\cup \{\texttt{query}(m)\}
      \end{align*}
      Since $M$ contains $ready(a,t)$ for all actions, the integrity constraint in Line 7 of Listing \ref{relaxed_exists} is satisfied. Furthermore, all state transition constraints are met by the definition of $s_t$.
      Therefore, $M$ is a stable model of $P^R(m)$.
    \item[($\Leftarrow$):]
      Assume that $M$ is a stable model of $P^p(m)$ such that $\{\langle a, t \rangle \mid \texttt{occurs}(a,t) \in M \} = \{\langle a, t \rangle \mid a \in A_t, 1 \le t \le m \}$. \\
      For any $1 \le t \le m$, the choice rule in Line 6 of Listing \ref{core} ensures that the set of \texttt{holds}-atoms induces a state function $s_t$ defined by $s_t(X) = V \Leftrightarrow \texttt{holds}(X,V,t) \in M$.
      
      Moreover, the rules and integrity constraints in $S(t)$ dictate the state transition. Specifically, the integrity constraint in Line 8 of Listing \ref{core} ensure that for each $a \in A_t$ and $X \in \dom(a^e)$, $s_t(X) = a^e(X)$ holds. Since $M$ is a stable model, contradicting effects on the same variable are impossible (as they would violate the constraints), which implies that $A_t$ is confluent.
      For unaffected fluents, the integrity constraint in Line 9 of Listing \ref{core} yield $s_t(X) = s_{t-1}(X)$ for each $X \in \mathcal F \setminus \bigcup_{a \in A_t} \dom(a^e)$. 
      
      Finally, given the fact \texttt{query}(m), the integrity constraint in $Q(m)$ ensures that $s_m(X) = s_*(X)$ for all $X \in \dom(s_*)$, satisfying the goal condition.
      
      We now distinguish the different cases to show that $A_t$ is $\forall$-/$\exists$-/\textit{relaxed} $\exists$-step serializable in $s_{t-1}$ based on the specific constraints of each encoding.

      \textbf{Case 1: } To show: $A_t$ is $\forall$-step serializable. \\
      Let $\langle a_1, \dots, a_k \rangle$ be an arbitrary sequence of actions such that $\{a_1, \dots, a_k\}=A_t$.
      We prove by induction that for all $1 \le i \le k$, the transition $o(\langle a_1, \dots, a_i \rangle, s_{t-1})$ is defined.
      \textit{Base case:} $i=1$: \\ 
      The integrity constraint checking preconditions (Line 1 of Listing \ref{forall}) guarantees that $a_1$ is applicable in $s_{t-1}$, i.e., $o(a_1, s_{t-1})$ is defined. \\

      \textit{Inductive step} $i \mapsto i+1$: \\ 
      Assume $o(\langle a_1, \dots, a_i \rangle, s_{t-1})$ is defined. We must show that $a_{i+1}$ is applicable in the resulting intermediate state $s_{temp} = o(\langle a_1, \dots, a_i \rangle, s_{t-1})$.
      Consider any fluent $X$ in the precondition of $a_{i+1}$. 
      If $X \notin \bigcup_{1 \le j \le i} \dom(a_j^e)$, then no preceding action has modified $X$. Thus $s_{temp}(X) = s_{t-1}(X)$, and the precondition holds trivially because $a_{i+1}$ is applicable in $s_{t-1}$.
      Therefore, we focus on the critical case where $X \in \dom(a_{i+1}^c) \cap \bigcup_{1 \le j \le i} \dom(a_j^e)$.
      This implies there exists a preceding action $a_j$ that has a postcondition on $X$. We distinguish two cases based on the effect of $a_{i+1}$ itself:
      \begin{enumerate}[label=(\roman*)]
        \item $X \notin \dom(a_{i+1}^e)$: \\
        The integrity constraint in Line 2 of Listing \ref{forall} requires that if $a_{i+1}$ has a precondition on $X$ but no postcondition, the value of $X$ cannot change in the final state $s_t$. Since $a_j$ writes $X$, we have $s_t(X) = a_j^e(X)$. The constraint implies $s_{t-1}(X) = s_t(X)$, meaning $a_j$ effectively did not change the value relative to $s_{t-1}$. Thus $s_{temp}(X) = a_{i+1}^c(X)$.
        \item $X \in \dom(a_{i+1}^e)$: \\
        The integrity constraint in Line 4 of Listing \ref{forall} ensures that if an action ($a_{i+1}$) has a precondition on $X$ and sets a conflicting postcondition ($a^c(X) \neq a^e(X)$), it is marked as \texttt{single}. However, since another action $a_j$ also acts on $X$ (as established by the intersection), the integrity constraint in Line 5 would be violated.
        Since $M$ is a stable model, this implies that $\texttt{single}(X, t) \notin M$ and $s_t(X) = a_{i+1}^e(X) = a_{i+1}^c(X)=s_{t-1}(X)$ must be the case.
      \end{enumerate} 
      In both cases, $s_{temp}(X) = a_{i+1}^c(X)$, so $o(\langle a_1, \dots, a_{i+1} \rangle, s_{t-1})$ is defined.
      Therefore, by induction, the entire sequence is valid, and $A_t$ is $\forall$-step serializable. \\

      \textbf{Case 2: } To show: $A_t$ is $\exists$-step serializable. \\
      The integrity constraint in Line 1 of Listing \ref{exists} guarantees that every action $a \in A_t$ is applicable in $s_{t-1}$, i.e., $o(a, s_{t-1})$ is defined.
      Furthermore, the integrity constraint in Line 6 ensures that $\texttt{ready}(a, t) \in M$ for each $a \in \mathcal O$ and the rules in Lines 4-5 that $\texttt{apply}(a,t) \in M$ for all $a \in A_t$.
      The rule defining \texttt{apply} (Lines 2-3) imposes a dependency: an action $a$ can only be applied if all actions $a'$ whose preconditions it invalidates are already \texttt{ready}.
      Since $M$ is a stable model, there exists a derivation sequence of the \texttt{apply} atoms that respects these dependencies. Let $\langle a_1, \dots, a_k \rangle$ be the sequence of actions in $A_t$ ordered by their derivation step.
      Consider any action $a_i$ in the sequence and a fluent $X$ in the precondition.
      Suppose that a preceding action $a_j$ ($j < i$) invalidates this precondition, i.e., $a_j^e(X) \neq a_i^c(X)$.
      By the definition of \texttt{apply} in the Lines 2-3, $a_j$ would have to wait for $a_i$ to become \texttt{ready} before it could be applied. This would imply that $a_i$ is derived before $a_j$, contradicting $j < i$.
      Therefore, for all $j < i$, $a_j$ does not invalidate $a_i$. Thus, $s_{t-1}(X)$ is either preserved ($s_{t-1}(X)=s_t(X)$) or modified confluently ($a_j^e(X) = a_i^c(X)$), ensuring $o(\langle a_1, \dots, a_k \rangle, s_{t-1})$ is defined. \\

      Regarding the alternative encoding using the \texttt{\#edge} directive (Listing \ref{exists_alt}), the graph logic ensures serializability explicitly.
      For any $1 \le t \le m$, the directive defines the conflict graph $G_t = (A_t, E_t)$ where a directed edge $(a, a') \in E_t$ exists iff action $a$ invalidates a precondition of action $a'$. 
      Since $P^E(m)$ enforces that $G_t$ is acyclic, there exists a sequence $\langle a_1, \dots, a_k\rangle$ comprising the actions in $A_t$ such that no edge connects any predecessor to a successor. Formally, for any $a_i$ in the sequence, there is no $a_j$ with $j < i$ such that $(a_j, a_i) \in E_t$.
      This absence of destructive edges from the past implies that for any $X$ in the precondition, either no predecessor changes $X$ or it is modified consistently, i.e.
      $s_{t-1}(X) = a_i^c(X)$ or $a_j^e(X) = a_i^c(X)$.
      Thus, $o(\langle a_1, \dots, a_i \rangle, s_{t-1})$ is defined for all $i$, and in particular $o(\langle a_1, \dots, a_k \rangle, s_{t-1})$ is defined, verifying that $A_t$ is $\exists$-step serializable. \\

      \textbf{Case 3: } To show: $A_t$ is \textit{relaxed} $\exists$-step serializable. \\
      In view of the rule in Line 1 of Listing \ref{relaxed_exists}, a fluent value is reachable if $s_{t-1}(X)=V$, i.e., $reach(X, V, t) \in M$.
      The recursive structure of the rules for \texttt{apply} (Lines 3-4) and \texttt{reach} (Line 2) imposes a dependency ordering: An action $a$ is only derived as \texttt{apply}(a,t) if all its preconditions are already in the extension of the \texttt{reach} predicate.

      Since $M$ is a stable model, there exists a finite derivation sequence for all atoms in $M$. Let $\langle a_1, \dots, a_k \rangle$ be the sequence of actions in $A_t$ ordered by the step in which their corresponding \texttt{apply} atom was derived (or satisfied).
      We prove that this sequence is a valid serialization.

      Consider any action $a_i$ in this sequence and any fluent $X$ in the precondition.
      The derivation of \texttt{apply}($a_i$,t) requires that \texttt{reach}($X, a_i^c(X), t$) was previously derived. By the definition of the \texttt{reach} rules, this value must originate either from:
      \begin{enumerate}[label=(\roman*)]
        \item The state $s_{t-1}$ (Line 1), which implies $s_{t-1}(X) = a_i^c(X)$, or
        \item The effect of a previously applied action $a_j$ (Line 2), which implies $a_j \in \{a_1, \dots, a_{i-1}\}$ and $a_j^e(X) = a_i^c(X)$.
      \end{enumerate}
      In both cases, the precondition of $a_i$ is satisfied by the state constructed from the initial state $s_{t-1}$ and the effects of the predecessors $\langle a_1, \dots, a_{i-1} \rangle$.
      Thus, $o(\langle a_1, \dots, a_k \rangle, s_{t-1})$ is defined.
  \end{itemize}
\end{proof}

\chapter{Method} \label{method}

\section{Pipeline Overview}
\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{Resources/pipeline.drawio.png}
  \caption{Pipeline for testing the different planning encodings}
  \label{pipeline}
\end{figure}


To systematically evaluate and compare the performance of the proposed parallel planning encodings against each other and the sequential baseline, we implemented an automated pipeline using the \texttt{lab} framework \parencite{seipp-et-al-zenodo2017}.
The benchmark\footnote{\url{https://github.com/aibasel/downward-benchmarks}} set consists of the unofficial collection of IPC instances provided by the Fast Downward project.

As illustrated in Figure \ref{pipeline}, the workflow proceeds in four main stages:
\begin{enumerate}
    \item \textbf{Preprocessing:} Starting with a planning task defined in PDDL (Planning Domain Definition Language), we use \texttt{cpddl} \parencite{fiser_2025_16423302} to convert the problem into a Finite Domain Representation (FDR).
    \item \textbf{Translation:} The \texttt{plasp} tool translates this FDR specification into a set of ASP facts. A detailed description of this translation process can be found in \textcite{DIMOPOULOS_2019}.
    \item \textbf{Solving:} In the core step, \textit{clingo} combines these facts with our modified incremental encodings (sequential, $\forall$-step, $\exists$-step, or \textit{relaxed} $\exists$-step). The solver then searches for stable models corresponding to valid plans.
    \item \textbf{Validation:} Finally, to ensure correctness, the resulting ASP-based plan (represented by the \texttt{occurs} atoms) is converted back into a sequential plan format. We employ the post-processing encoding provided by \textit{plasp} for serialization and subsequently format the output to be readable by the plan validator VAL\footnote{\url{https://github.com/KCL-Planning/VAL}}.
\end{enumerate}

Each component of this pipeline involves specific configurations and optimizations, which will be detailed in the following sections.


\section{Encoding Adaptation}
To integrate the theoretical encodings from \textcite{DIMOPOULOS_2019} presented in Section \ref{asp encoding} into the automated pipeline, small changes are required to bridge the gap between the \texttt{plasp} output format and the internal predicates used by our logic programs.
The necessary adaptations are shown in Listing \ref{core_mod}.
\begin{listing}[H]
\caption{Modifications to the core encoding}
\label{core_mod}
\begin{minted}[frame=single,framesep=2mm,baselinestretch=1.2,linenos]{prolog}
#include <incmode>.
#program base.
fluent(X) :- variable(X).
fluent(X,V) :- initialState(X,V), fluent(X).
value(X,V) :- contains(X, V).
prec(A, X, V) :- precondition(A, X, V).
post(A, X, V) :- postcondition(A, _, X, V).
...
:- fluent(X), #count{V : holds(X,V,0)} > 1.
...
#program step(t).
...
#show.
#show occurs(A, t): occurs(A, t).
\end{minted}
\end{listing}

Lines 3-6 map the predicates generated by \textit{plasp} (e.g., \texttt{initialState}, \texttt{contains}) to the predicates used in our encodings.
Line 7 handles the action effects. 
By using the wildcard \texttt{\_} for the second argument, we discard the specific effect identifiers. This simplification is chosen to reduce the complexity for the solver: explicitly tracking every effect could inflate the ground program and force \textit{clingo} to manage a significantly larger set of atoms, which could degrade performance without necessarily improving the quality of the resulting plans.
Line 9 performs a sanity check to ensure that the initial state is consistent (each fluent has at most one value).
Finally, Lines 13-14 act as an output filter, ensuring that only the relevant \texttt{occurs} atoms are displayed in the final answer sets.

\subsection{Post-Processing and Serialization}
The stable models computed by \textit{clingo} are output as sets of atoms (e.g., \texttt{occurs(action("move","a","b"), 1)}). 
Standard plan validators like \textit{VAL}, however, require plans in a specific text-based format (often referred to as SAS plan or IPC format) structured as \texttt{time: (action args)}.
To bridge this gap, we implemented a lightweight post-processing module in Python.
Briefly, the script iterates through the output string to identify all \texttt{occurs} atoms using regular expressions. 
For each match, it extracts the time step $t$ and the action arguments, stripping internal quotes and formatting delimiters. 
Finally, the extracted pairs are written to a file in a line-by-line format compatible with the validator.
This conversion ensures that the parallel plans found by our encodings can be verified against the original PDDL domain and problem specification.

\section{Optimizations}
\subsection{Mutexes}
To further optimize the solving process, we utilize invariant constraints derived during the translation process. 
Specifically, we incorporate \textit{mutex groups} (mutually exclusive sets of facts) provided by the \textit{cpddl} preprocessor.

\begin{defi}[mutex and mutex groups]
  A set of facts $M \subseteq \mathcal F$ is called 
  \begin{enumerate}[label=(\roman*)]
    \item a \textit{mutex} $~~~~~~~\logeq$ for all reachable states $s: M \not\subseteq s$.
    \item a \textit{mutex group} $\logeq$ for all reachable states $s: | M \cap s | \le 1$.
  \end{enumerate}
\end{defi}

Intuitively, a mutex group represents a set of facts where no two facts can be true simultaneously. Enforcing these invariants as integrity constraints can significantly prune the search space by detecting invalid partial states early.
We extend our encoding with the rules shown in Listing \ref{mutex}.

\begin{listing}[H]
\caption{Expansion for mutexes}
\begin{minted}[frame=single,framesep=2mm,baselinestretch=1.2,linenos]{prolog}
#program base.
...
mutex(G,X) :- mutexGroup(G), contains(G,X,V), fluent(X,V).
mutex(G)   :- mutexGroup(G), #count{X : mutex(G,X)} > 1.
:- mutex(G), #count{X,V : holds(X,V,0), contains(G,X,V)} > 1.
...
#program step(t).
...
:- mutex(G), #count{X,V : holds(X,V,t), contains(G,X,V)} > 1.
...
\end{minted}
\label{mutex}
\end{listing}

The mutex information is provided by \textit{plasp} via the predicates \texttt{mutexGroup/1} and \texttt{contains/3}.
In the base program, we first define which fluents belong to a mutex group (Line 3), where a group must contain at least two fluents (Line 4).
The integrity constraint in Line 5 ensures that no more than one fluent of each mutex group holds in the initial state and the integrity constraint in Line 9 ensures the same for each state in timestep $t$.


\subsection{Guess and Check Optimization}
A crucial optimization for the $\exists$-step encodings is the \textit{Guess and Check} approach. 
The core idea is to decouple the search for action sets from the computationally expensive verification of acyclicity.
Instead of enforcing serializability directly during the search, we first "guess" a candidate solution that satisfies all local constraints (preconditions, effects, confluence) but ignores potential cycles. In a second step, we "check" this candidate for circular invalidations.

Formally, we decompose the problem into a pair of logic programs $\langle G, C \rangle$, where $G$ is the \textit{generator} or \textit{guesser} and $C$ is the \textit{checker}.
A stable model $M$ of $G$ represents a valid plan if and only if the checker program, when grounded with the actions from $M$, is \textbf{unsatisfiable} (i.e., no cycle can be derived).

To implement the generator $G$, \textcite{DIMOPOULOS_2019} proposes using the core program ($S(t)$ from Listing \ref{core}) combined with the precondition checks (Line 1 of Listing \ref{seq}, \ref{forall}, or \ref{exists}) and the problem instance facts.
Consequently, a stable model $M$ of $G$ guarantees that:
\begin{enumerate}
    \item Preconditions hold at $t-1$ for all chosen actions.
    \item Postconditions are applied and confluent (via Line 8 of Listing \ref{core}).
\end{enumerate}
The only property remaining to be verified is the absence of circular invalidations (cycles in the conflict graph), where actions invalidates each other's preconditions (e.g., $a^e(X) \neq a'^c(X)$ and $a'^e(X') \neq a'^c(X')$).

This verification is performed by the program $C$, shown in Listing \ref{check}. 
The inputs to $C$ are the action occurrences (\texttt{occurs/2}) from the candidate model $M$, along with the problem instance facts.

\begin{listing}[H]
  \caption{Encoding for detecting circular invalidation (Checker)}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos]{prolog}{encodings/EX_check.lp}
  \label{check}
\end{listing}
The checker works by attempting to construct a valid serialization order using the predicate \texttt{ready}:
\begin{itemize}
    \item Like in the encoding of Listing \ref{exists}, Lines 7-11 try to derive \texttt{ready}$(A,t)$ for all actions that can be safely applied.
    \item Line 12 derives the atom \texttt{cycle}$(t)$ if there exists any occurring action $A$ that never becomes \texttt{ready}. This indicates that $A$ is part of a invalidation cycle.
    \item Crucially, the program is designed to be \textbf{satisfiable} only if a cycle exists (Line 3).
\end{itemize}

Thus, if $C \cup M$ is satisfiable, a circular invalidation exists at some step $t$, meaning no valid permutation of the actions $A_t$ exists. This implies the candidate is not serializable under the $\exists$-step semantics and must be rejected.
Conversely, if $C \cup M$ is unsatisfiable, no cycle was found, and the candidate $M$ is a valid plan.


\subsection{Heuristic}
To further optimize the solving process, we employ domain-independent heuristics using the \texttt{\#heuristic} directive of \textit{clingo}.
Following the approach described by \textcite{DIMOPOULOS_2019}, we modify the solver's search strategy by assigning specific attributes to atoms:
\begin{itemize}
  \item \textit{level} ($\in \mathbb{Z}$, default $0$): Determines the decision priority. The solver selects atoms with higher levels before those with lower levels.
  \item \textit{sign} ($\in \mathbb{Z}$, default $0$): Determines the preferred truth value. A positive value ($>0$) biases the solver to assign \textit{true}, a negative value ($<0$) to assign \textit{false}. If the value is $0$, the solver falls back to its default \textit{sign} heuristic (e.g., VSIDS).
\end{itemize}

Listing \ref{heuristic} implements a \textit{backward propagating} heuristic.
The core idea is to leverage the goal condition and bias the search towards establishing the goal fluents as early as possible in the plan.
\begin{listing}[H]
  \caption{Backward propagating heuristic}
  \inputminted[frame=single,framesep=2mm,baselinestretch=1.2,linenos]{prolog}{encodings/heuristic.lp}
  \label{heuristic}
\end{listing}

The priority level is defined as $l = 2^{31}-1-t$ (the largest integer supported by \textit{clingo}), assigning the highest priority to the earliest time steps (since smaller $t$ yields a larger $l$).
The logic in Lines 2 and 3 effectively projects state information backwards from $t$ to $t-1$:
\begin{itemize}
    \item \textbf{Positive Bias} (Line 2): If \textit{clingo} determines that $holds(X,V,t)$ is true, the heuristic activates and suggests setting $holds(X,V,t-1)$ to true as well.
    \item \textbf{Negative Bias} (Line 3): Conversely, if $holds(X,V,t)$ becomes false, the heuristic biases $holds(X,V,t-1)$ to be false.
\end{itemize}
Intuitively, this encourages the solver to satisfy the goal state $s_*$ by assuming (unless forced otherwise by actions) that the desired fluents already held in the previous step $t-1$. This creates a "pull" effect from the goal backwards to the initial state $s_0$.


\section{Experimental Setup}
To evaluate the performance of the different planning encodings and the effectiveness of the proposed optimizations, we performed a systematic comparison across four distinct experimental configurations.

\subsection{Configurations and Encodings}
We tested the sequential encoding alongside the $\forall$-step, both $\exists$-step and \textit{relaxed} $\exists$-step encodings. 
Additionally, we evaluated the \textit{Guess and Check} strategy with a fallback mechanism: if the Guess and Check approach fails to find a valid serializable plan (or implies a cycle), the system automatically falls back to a safe encoding (e.g. $\forall$-step). In such cases, the reported runtime is the sum of the time spent on the failed check and the time required by the fallback strategy. If this cumulative time exceeds the time limit, the instance is considered unsolved.

To isolate the impact of our optimizations, we ran the complete benchmark set in four configurations, varying the usage of mutexes and heuristics:
\begin{enumerate}
    \item \textbf{Baseline:} No extra mutex detection, default solver heuristics.
    \item \textbf{Mutexes (h2):} \textit{cpddl} enabled with the $h^2$ option to compute additional mutex groups.
    \item \textbf{Heuristics:} Using the backward propagating heuristic (Listing \ref{heuristic}).
    \item \textbf{Combined:} Both $h^2$ mutexes and backward heuristics enabled.
\end{enumerate}

\subsection{Benchmark Selection and Validation Criteria}
\textbf{Validation Criteria:}
For a planning task to be considered reliably \textit{solved}, finding a candidate solution via \textit{clingo} is necessary but not sufficient. 
We enforce a strict validation protocol: A run counts as solved if and only if:
\begin{enumerate}
    \item The solver produces an answer set within the 30-minute time limit.
    \item The extracted plan is successfully validated by the external validator \textit{VAL}.
\end{enumerate}
Any run where \textit{clingo} reports a solution that \textit{VAL} subsequently rejects is treated as an error and classified as unsolved.

\textbf{Benchmark Domains:}
We evaluated our approach on a suite of benchmarks from the International Planning Competitions (IPC). 
The test set comprises domains used in previous competitions (e.g., IPC 1998-2018), specifically selected in their STRIPS formulations to ensure compatibility with our encodings.
The selection covers a wide spectrum of planning complexity:
\begin{itemize}
    \item \textit{Classical Domains:} Standard benchmarks such as \textit{Blocks}, \textit{Logistics}, \textit{Gripper}, and \textit{Driverlog}.
    \item \textit{Constraint-Heavy Domains:} Complex puzzles like \textit{Sokoban}, \textit{Freecell}, and \textit{Parking}.
    \item \textit{Recent Competitions:} Modern domains including \textit{Agricola} (IPC 2018), \textit{Snake} (IPC 2018), and \textit{Quantum Layout} (IPC 2023).
\end{itemize}
This diverse set allows us to analyze the performance of parallel encodings across different problem structures (e.g., transportation, manipulation, construction).

\subsection{Computational Environment}
The experiments were executed on the \textit{bwUniCluster 3.0} at the Karlsruhe Institute of Technology (KIT). 
We utilized compute nodes equipped with Intel Xeon Gold 6248R CPUs (3.0 GHz, 24 cores).
To ensure comparability, each run was restricted to a single CPU core.
We imposed a strict time limit of 30 minutes (1800 seconds) and a memory limit of 32 GiB for the execution of \textit{clingo} and \textit{cpddl} each.
Runs exceeding these limits were aborted and recorded as unsolved (time-out).


\chapter{Results} \label{result}
\section{Overall Performance}
To get an overview of the performance of the different encodings, we first analyze the aggregated results across all benchmark domains.
In this section, we focus exclusively on the \textit{baseline} configurations to establish a fundamental comparison. 
We compare the Sequential encoding ($S$) against the $\forall$-step ($\forall$), the $\exists$-step ($\exists$), the graph-based $\exists$-step ($E$), the \textit{relaxed} $\exists$-step ($R$), and the Guess-and-Check strategy ($G\&C$).

Figure \ref{fig:solved} illustrates the coverage (total number of solved instances) for each domain. For better readability, the data is split into two charts. Domains where no instances were solved by any algorithm are omitted.
Figure \ref{fig:time} displays the average runtime. To ensure a fair comparison, this metric is calculated only on the intersection of instances that were successfully solved by \textit{all} algorithms. Consequently, domains where no common set of solved instances exists are excluded from the runtime analysis.


\begin{figure}[t]
  \centering
  % Part 1
  \includegraphics[width=\textwidth]{../data/ParallelASPPlanning-eval/solved_part1.png}
  \vspace{0.2cm}
  % Part 2
  \includegraphics[width=\textwidth]{../data/ParallelASPPlanning-eval/solved_part2.png}
  \caption{Coverage analysis: Number of solved instances per algorithm across benchmark domains. The heatmap indicates the absolute number of solved instances (left) out of the total available instances (right).}
  \label{fig:solved}
\end{figure}

\begin{figure}[t]
  \centering
  % Part 1
  \includegraphics[width=\textwidth]{../data/ParallelASPPlanning-eval/avg_time_part1.png}
  \vspace{0.2cm}
  % Part 2
  \includegraphics[width=\textwidth]{../data/ParallelASPPlanning-eval/avg_time_part2.png}
  \caption{Runtime analysis: Average solving time (in seconds) for successfully solved instances. Red cells indicate higher runtimes, while light yellow cells indicate faster performance.}
  \label{fig:time}
\end{figure}

\subsection{Coverage Analysis}
The coverage results in Figure \ref{fig:solved} reveal several trends regarding the effectiveness of parallel planning.

\paragraph{Sequential vs. Parallel Performance:}
In domains that allow for a high degree of concurrency, parallel encodings significantly outperform the sequential approach.
Notable examples include \textit{airport}, \textit{woodworking}, and both \textit{logistics} variants. 
For instance, in \textit{woodworking}, the sequential encoding ($S$) solves only 7 out of 50 instances, whereas all parallel encodings (with the exception of Guess-and-Check) solve the entire set.
Similarly, in \textit{floortile}, the sequential encoding fails to solve any instance (0/40), while the \textit{relaxed} $\exists$-step encoding achieves a coverage of 38/40. 
This confirms that the reduced time horizon $T$ enabled by parallel semantics can effectively mitigate the search depth explosion inherent to sequential planning.
However, in more complex or inherently sequential domains like \textit{agricola} or \textit{snake}, this performance advantage is not observed.

\paragraph{Comparison of Parallel Encodings:}
Comparing the different parallel semantics reveals a trade-off between \textit{plan flexibility} (allowing shorter horizons) and \textit{encoding complexity} (grounding and solving overhead):
\begin{itemize}
    \item \textbf{$\forall$-step ($\forall$):} Although it enforces the strictest requirements for parallel execution, the $\forall$-step encoding shows better performance in several domains (e.g., \textit{tetris}, \textit{parcprinter}), often outperforming even the more flexible variants in domains like \textit{hiking} or \textit{scanalyzer}.
    This suggests that for many IPC domains, the capability to interleave dependent actions is not critical. Consequently, the advantage of the $\forall$-step semantics lies in its compactness: it requires fewer constraints and auxiliary atoms than the $\exists$-step variants, thereby significantly reducing the burden on the grounder and solver \parencite{DIMOPOULOS_2019}.

    \item \textbf{$\exists$-step ($\exists$ /E):} The strength of the $\exists$-step semantics is seen in domains with complex dependencies. A prime example is \textit{floortile}, where the strict $\forall$-step encoding solves only 18 out of 40 instances, whereas the $\exists$-step encoding achieves a coverage of 37/40. 
    This demonstrates that the increased flexibility of the $\exists$-step semantics enables the discovery of plans in constrained environments where the conservative $\forall$-step approach fails. 
    Regarding the implementation variants, the graph-based alternative ($E$) generally performs on par with or marginally better than the standard constructive encoding ($\exists$), showing no significant deviation in total coverage.

    \item \textbf{\textit{Relaxed} $\exists$-step (R):} The relaxed encoding typically yields lower coverage compared to the standard $\exists$-step variants, as observed in domains like \textit{quantum-layout}. Although the \textit{relaxed} $\exists$-step encoding theoretically enables the shortest plan horizons by allowing actions to support each other within the same step, the computational cost is significant.
    The recursive definition of the reachability predicates (\texttt{reach/3}) probably increases the complexity of the ground program.
    Consequently, the overhead of solving these dense constraints often outweighs the benefits gained from a reduced time horizon.
    
    \item \textbf{Guess-and-Check ($G\&C$):} The performance of the Guess-and-Check strategy is generally inferior to the direct encodings ($\exists$ and $E$). 
    Although separating the cycle check from the plan generation reduces the complexity of the generator, the overhead of invoking the solver twice significantly impacts runtime.
    Crucially, when the validity check fails (i.e., the guessed plan contains a cycle), the pipeline must trigger the fallback mechanism. 
    In these cases, the runtime accumulates (time for failed G\&C + time for fallback), often leading to timeouts on instances that the pure $\forall$-step or $\exists$-step encodings could solve directly within the time limit.
    Consequently, this strategy proves less robust for general-purpose planning compared to the integrated approaches.
\end{itemize}


\subsection{Runtime Analysis}

\subsubsection{Overview across Domains}
Figure \ref{fig:time} provides an overview of the average runtimes across the evaluated domains.
The sequential encoding ($S$) generally exhibits significantly higher runtimes compared to the parallel approaches. This discrepancy is particularly pronounced in domains with high concurrency potential, such as \textit{Gripper}, where the average runtime for $S$ is approximately 430 seconds, whereas the $\forall$-step encoding requires less than 3 seconds. This confirms that the reduced plan horizon in parallel planning typically translates to reduced solving time.

Among the parallel variants, the $\forall$-step encoding consistently achieves the lowest runtimes, rarely exceeding 100 seconds even on harder instances. 
This performance advantage can be attributed to the compactness of the encoding, which imposes the least burden on the grounder and solver.

The $\exists$-step encodings ($\exists$ and $E$) display higher variability. While they allow for shorter plan lengths, the increased complexity of the compatibility checks often leads to longer solving times compared to $\forall$-step, particularly in dependency-heavy domains like \textit{Freecell}. 

\subsubsection{Detailed Pairwise Comparison}
To investigate these performance characteristics and implementation differences in greater detail, Figure \ref{fig:scatter_matrix} provides pairwise scatter plots on common solved instances.

\paragraph{$\exists$-step Implementation Variants (Fig. \ref{fig:exist_vs_exist_edge}):}
Comparing the two implementation variants reveals a distinct advantage for the graph-based approach. 
As illustrated in Figure \ref{fig:exist_vs_exist_edge}, the alternative $\exists$-step encoding ($E$) outperforms the standard constructive encoding ($\exists$) in the majority of instances. 
Notably, in the \textit{Nomystery} domain, $E$ achieves a runtime reduction of over 200 seconds compared to $\exists$ on the largest instances, validating the efficiency of the \texttt{\#edge} constraints.

\paragraph{$\forall$-step vs. $\exists$-step (Fig. \ref{fig:forall_vs_exists}):}
The direct comparison between the two main parallel semantics highlights the trade-off mentioned above. 
Although the $\exists$-step encoding is theoretically more flexible, the scatter plot shows a cluster of points above the diagonal. 
This confirms that the $\forall$-step encoding is frequently faster, suggesting that the overhead of grounding the complex $\exists$-step compatibility constraints outweighs the benefits of a slightly reduced horizon.

\paragraph{$\forall$-step vs. Guess-and-Check (Fig. \ref{fig:forall_vs_guess_and_check}):}
Finally, the comparison with the Guess-and-Check strategy ($G\&C$) challenges the assumption that the iterative approach necessarily incurs a high overhead.
As shown in Figure \ref{fig:forall_vs_guess_and_check}, the performance is largely on par with the $\forall$-step encoding, with $G\&C$ frequently achieving even faster runtimes (points situated below the diagonal).
This efficiency can be attributed to the reduced complexity of the \textit{generator} component. 
Since the generator ignores circular dependency constraints, it produces a significantly smaller ground program than the full $\forall$-step encoding. 
In domains where circular conflicts are rare, the first "guessed" plan is often valid. 
Consequently, the combined runtime of the lightweight generator and the subsequent check is often lower than solving the stricter, more complex constraints of the $\forall$-step encoding directly.

\begin{figure}[t]
  \centering
  \begin{subfigure}[t]{0.48\textwidth} 
    \centering
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/algo_comparison/no modifications/sequential_vs_forall.png}
    \caption{$S$ vs $\forall$ encoding}
    \label{fig:sequential_vs_forall}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/algo_comparison/no modifications/forall_vs_exists.png}
    \caption{$\forall$ vs $\exists$ encoding}
    \label{fig:forall_vs_exists}
  \end{subfigure}
  
  \vspace{0.2cm} % Reduziert von 0.5cm auf 0.2cm
  
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/algo_comparison/no modifications/exists_vs_exists_edge.png}
    \caption{$\exists$ vs $E$ encoding}
    \label{fig:exist_vs_exist_edge}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/algo_comparison/no modifications/forall_vs_guess_and_check.png}
    \caption{$\forall$ vs $G\&C$ encoding}
    \label{fig:forall_vs_guess_and_check}
  \end{subfigure}
  
  \caption{Runtime comparison between different encodings. Each point represents an instance solved by both encodings, with the x-coordinate indicating the runtime of the first encoding and the y-coordinate that of the second. Points below the diagonal indicate better performance of the first encoding.}
  \label{fig:scatter_matrix}
\end{figure}


\section{Impact of Optimizations}
To evaluate the effectiveness of the implemented optimizations, we compared the baseline encoding against configurations equipped with $h^2$-mutexes, the backward-propagating heuristic, and a combination of both. Figure \ref{fig:scatter_matrix_opti} provides a detailed runtime comparison of these configurations.

\paragraph{Heuristic Guidance (Fig. \ref{fig:base_vs_h}):}
Contrary to expectations, the backward-propagating heuristic did not yield a significant improvement in coverage or runtime. 
As illustrated in Figure \ref{fig:base_vs_h}, the data points align almost perfectly with the diagonal, indicating negligible deviation from the baseline performance. 
This suggests that the search from the goal backwards do not substantially reduce the search time compared to \textit{clingo}'s internal heuristic.

\paragraph{Impact of more Mutexes (Fig. \ref{fig:base_vs_s_m_clingo} \& \ref{fig:base_vs_s_m_total}):}
In contrast, the inclusion of the $h^2$-algorithm for \textit{cppdl} resulted in a measurable performance gain. The impact is particularly pronounced in highly constrained domains such as \textit{Agricola}. In this domain, the sequential encoding solves 7 instances (compared to only 1 in the baseline) and the $\forall$-step encoding improves from 0 to 5 solved instances.

Analyzing the runtime reveals a distinct trade-off between solving power and preprocessing overhead:
\begin{itemize}
    \item \textbf{Solver Efficiency (Fig. \ref{fig:base_vs_s_m_clingo}):} When looking exclusively at the solver runtime (\textit{clingo} time), the additional mutexes provide a drastic speedup. Most points lie well below the diagonal, showing that the mutex constraints effectively prune the search space.
    \item \textbf{Total Runtime (Fig. \ref{fig:base_vs_s_m_total}):} When accounting for the full pipeline duration (including \textit{cpddl} preprocessing), the picture changes. 
    For many instances, the overall runtime does not significantly improve. In fact, for easier instances, the overhead of longer searching for mutexes outweighs the time saved during the plan search, leading to a longer total runtime compared to the baseline.
    This effect is particularly pronounced in the $\forall$-step encoding, which is already compact and thus benefits less from additional pruning.
    In comparison, for the \textit{relaxed} $\exists$-step encoding, the total runtime with more mutexes is often lower than the baseline, indicating that "heavier" encodings profit more from the invariants.
\end{itemize}

\paragraph{Combined Configuration (Fig. \ref{fig:s_m_vs_h_s_m_total}):}
As expected, adding the heuristic on top of the deeper mutex search does not yield further improvements. 
Figure \ref{fig:s_m_vs_h_s_m_total} compares the encoding with the mutex optimization against the combined version. The almost perfect alignment with the diagonal confirms that the heuristic remains ineffective even in the presence of more mutexes.

Figure \ref{fig:s_m_vs_h_s_m_total} confirms that the system's performance is driven almost exclusively by the constraints generated by the $h^2$ algorithm.
\begin{figure}[t]
  \centering
  % Heuristik-Analysen ---
  \begin{subfigure}[t]{0.48\textwidth} 
    \centering
    % Baseline vs. Heuristic
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/modifications/baseline_vs_heuristic.png}
    \caption{Effect of Heuristic (Baseline vs. Heur.)}
    \label{fig:base_vs_h}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    % Mutex vs. Mutex + Heuristic
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/modifications/strong_mutex_vs_heuristic_strong_mutex_total_time.png}
    \caption{Heuristic added to Mutexes}
    \label{fig:s_m_vs_h_s_m_total}
  \end{subfigure}
  
  \vspace{0.3cm} 
  % --- Zeile 2: Mutex-Analysen ---

  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    % Baseline vs. Mutex (Clingo Time)
    \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/modifications/baseline_vs_strong_mutex_clingo_total_time.png}
    \caption{Mutex Impact: Solver Time Only}
    \label{fig:base_vs_s_m_clingo}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
      \centering
      % Baseline vs. Mutex (Total Time)
      \includegraphics[width=\textwidth, trim={0 0 4cm 0}, clip]{../data/comparison_experiment-eval/modifications/baseline_vs_strong_mutex_total_time.png}
      \caption{Mutex Impact: Total Time (incl. Prep.)}
      \label{fig:base_vs_s_m_total}
  \end{subfigure}
  
  \caption{Runtime comparison of optimization techniques. Top row: The heuristic shows negligible impact (points on diagonal). Bottom row: Mutexes significantly reduce solver time (c), but preprocessing overhead affects total time on easy instances (d).}
  \label{fig:scatter_matrix_opti}
\end{figure}


\section{Summary of Results}
The experimental evaluation leads to three main conclusions regarding the performance of ASP-based planning encodings:

\begin{enumerate}
    \item \textbf{Parallelism pays off:} Parallel encodings ($\forall$-step and $\exists$-step) significantly outperform the sequential baseline in almost all standard benchmarks. The reduction of the planning horizon $T$ is the primary driver for this efficiency.
    
    \item \textbf{Simplicity beats flexibility:} Among the parallel variants, the strict $\forall$-step encoding proves to be the most robust. While the $\exists$-step semantics theoretically allow for shorter plans, the associated grounding overhead often negates this advantage. However, when $\exists$-step is required, the graph-based implementation ($E$) is superior to the constructive approach.
    
    \item \textbf{Importance of Preprocessing:} The integration of invariants, specifically $h^2$-mutexes, is crucial for solving highly constrained instances. In contrast, simple domain-independent heuristics (like backward propagation) fail to provide a tangible benefit over the solver's built-in strategies.
\end{enumerate}

These findings suggest that future improvements should focus on compact encodings (like $\forall$-step) combined with rigorous preprocessing, rather than increasingly complex relaxed semantics.


\printbibliography
\end{document}